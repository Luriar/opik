# Airflow 2.10.0 / Python 3.12 compatible runtime.
# Keep these versions pinned: newer Hugging Face packages upgrade Click and
# prevent the Celery worker from starting.
boto3==1.34.131
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
aiohttp==3.10.3
pymupdf==1.24.10
numpy==1.26.4
pyarrow==17.0.0
pendulum==3.0.0

# Gold embedding runtime (multilingual-e5-small).
torch==2.3.1
sentence-transformers==3.0.1
transformers==4.41.2
huggingface-hub==0.23.4
tokenizers==0.19.1

# Airflow 2.10.0 constraints. Do not upgrade independently.
click==8.1.7
rich==13.7.1
